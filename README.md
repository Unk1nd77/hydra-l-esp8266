# Hydra-L - Метеостанция на ESP8266

**Hydra-L** - это компактная IoT метеостанция на базе ESP8266, предназначенная для мониторинга параметров окружающей среды с возможностью удаленного доступа и передачи данных.

## 🚀 Быстрая установка прошивки

### 🎯 Для новичков - СУПЕР ПРОСТОЙ способ:

1. **Скачайте проект:**
   ```bash
   git clone https://github.com/Unk1nd77/hydra-l-esp8266.git
   cd hydra-l-esp8266
   ```

2. **Установите окружение:**
   ```bash
   ./install_macos.sh    # macOS
   ./install_linux.sh    # Linux
   ```

3. **Подключите ESP8266 через USB и запустите:**
   ```bash
   ./flash_device.sh
   ```

**ВСЁ! Скрипт сам найдет устройство, соберет и прошьет его.**

---

### 🔧 Для опытных пользователей - пошаговый способ:

<details>
<summary>Нажмите для раскрытия подробных инструкций</summary>

### 1️⃣ Скачайте проект
```bash
git clone https://github.com/Unk1nd77/hydra-l-esp8266.git
cd hydra-l-esp8266
```

### 2️⃣ Установите окружение одной командой
```bash
# На macOS
./install_macos.sh

# На Linux
./install_linux.sh
```

### 3️⃣ Активируйте окружение
```bash
source ~/esp/setup_env.sh
```

### 4️⃣ Соберите прошивку
```bash
./build.sh
```

### 5️⃣ Подключите ESP8266 и прошейте
```bash
# Найдите порт устройства
ls /dev/cu.*        # macOS
ls /dev/ttyUSB*     # Linux

# Прошейте устройство (замените порт на ваш)
python3 $IDF_PATH/tools/idf.py -p /dev/cu.usbserial-* flash monitor
```

</details>

**После прошивки устройство создаст WiFi сеть "Hydra-L" (пароль: 12345678) и будет доступно по адресу http://192.168.4.1**

---

## 📋 Описание устройства

**Hydra-L** представляет собой автономную метеостанцию, которая:
- Измеряет температуру, влажность и атмосферное давление
- Отображает данные на LCD дисплее
- Передает показания на удаленный сервер через WiFi
- Предоставляет веб-интерфейс для мониторинга
- Работает в режиме точки доступа для настройки

## 🔧 Функциональные возможности

### Измерение параметров
- **Температура**: -40°C до +85°C (точность ±1°C)
- **Влажность**: 0-100% RH (точность ±3%)
- **Давление**: 300-1100 hPa (точность ±1 hPa)
- **Усреднение показаний** для стабильности данных

### Отображение информации
- **LCD дисплей 16x2** с подсветкой
- **3 режима отображения**:
  - Режим 0: Показания сенсоров
  - Режим 1: Пользовательский текст
  - Режим 2: Сетевая информация (IP адрес)

### Сетевые возможности
- **WiFi подключение** к домашней сети
- **Точка доступа** для настройки (SSID: Hydra-L)
- **Веб-сервер** с REST API
- **Автоматическая отправка данных** на сервер
- **Удаленное управление** через HTTP запросы

### Управление
- **2 физические кнопки**:
  - Кнопка 1: Переключение режимов LCD
  - Кнопка 2: Управление подсветкой
- **Веб-интерфейс** для удаленного управления
- **API для интеграции** с другими системами

## 🔌 Схема подключения

### Компоненты
- **ESP8266** (WeMos D1 Mini или аналог)
- **BME280** - датчик температуры, влажности и давления
- **LCD 1602 с I2C модулем** - дисплей для отображения данных
- **2 кнопки** - для управления устройством

### Подключение (WeMos D1 Mini)
```
BME280 → ESP8266
VCC    → 3.3V
GND    → GND
SDA    → D2 (GPIO 4)
SCL    → D1 (GPIO 5)

LCD I2C → ESP8266
VCC     → 5V
GND     → GND
SDA     → D2 (GPIO 4)
SCL     → D1 (GPIO 5)

Кнопки → ESP8266
Кнопка 1 → D6 (GPIO 12)
Кнопка 2 → D7 (GPIO 13)
```

## 🌐 API Endpoints

### Получение данных
- `GET /getData` - JSON с показаниями сенсоров
```json
{
  "temperature": 23.5,
  "humidity": 65.2,
  "pressure": 1013.2,
  "rssi": -45,
  "mac": "AA:BB:CC:DD:EE:FF",
  "ip": "192.168.1.100"
}
```

### Управление устройством
- `POST /setMode` - Установка режима LCD (0-2)
- `POST /setLCD` - Установка пользовательского текста
- `POST /setLED` - Управление подсветкой (0/1)

## 📁 Навигация по проекту

### Основные директории
```
hydra-l-esp8266/
├── main/                    # Основной код прошивки
├── components/              # Драйверы компонентов
├── docs/                    # Документация (создается автоматически)
├── scripts/                 # Скрипты сборки и установки
└── config/                  # Файлы конфигурации
```

### Детальная структура файлов

#### 📂 `main/` - Основной код
```
main/
└── main.c                   # Главный файл прошивки
    ├── app_main()          # Точка входа программы
    ├── WiFi управление     # Подключение к сети и AP режим
    ├── HTTP сервер         # Веб-сервер и API endpoints
    ├── Задачи FreeRTOS     # Многозадачность
    └── Обработка событий   # GPIO, кнопки, сенсоры
```

#### 📂 `components/` - Драйверы устройств

**BME280 драйвер:**
```
components/bme280/
├── bme280.c                 # Реализация драйвера
├── include/bme280.h         # Заголовочный файл
└── CMakeLists.txt          # Конфигурация сборки

Функции:
- bme280_init()             # Инициализация сенсора
- bme280_read()             # Чтение данных (T, H, P)
- Калибровка и компенсация  # Точные измерения
```

**LCD драйвер:**
```
components/lcd/
├── lcd.c                    # Реализация драйвера
├── include/lcd.h            # Заголовочный файл
└── CMakeLists.txt          # Конфигурация сборки

Функции:
- lcd_init()                # Инициализация дисплея
- lcd_print()               # Вывод текста
- lcd_clear()               # Очистка экрана
- lcd_set_cursor()          # Позиционирование
- lcd_backlight_on/off()    # Управление подсветкой
```

#### 📂 Конфигурационные файлы
```
├── CMakeLists.txt          # Основная конфигурация сборки
├── sdkconfig.defaults      # Настройки ESP8266 по умолчанию
├── Makefile               # Альтернативная система сборки
└── config_example.txt     # Пример файла конфигурации
```

#### 📂 Скрипты автоматизации
```
├── install_linux.sh       # Установка окружения разработки
├── build.sh               # Автоматическая сборка проекта
├── test_build.sh          # Тестирование сборки
└── upload_to_github.sh    # Загрузка на GitHub
```

#### 📂 Документация
```
├── README.md              # Основная документация (этот файл)
├── FIXES.md               # Описание исправлений и улучшений
├── SUMMARY.md             # Краткая сводка проекта
└── config_example.txt     # Пример конфигурации устройства
```

## ⚙️ Конфигурация и настройка

### Основные параметры (main/main.c)
```c
// WiFi настройки
#define WIFI_SSID      "your_ssid"        # Имя домашней сети
#define WIFI_PASS      "your_password"    # Пароль сети

// Точка доступа для настройки
#define AP_SSID        "Hydra-L"          # Имя точки доступа
#define AP_PASS        "12345678"         # Пароль точки доступа

// I2C пины (WeMos D1 Mini)
#define I2C_MASTER_SCL_IO    2            # GPIO2 (D4) - Clock
#define I2C_MASTER_SDA_IO    14           # GPIO14 (D5) - Data

// Управляющие кнопки
#define BUTTON_1_GPIO        12           # GPIO12 (D6) - Режимы LCD
#define BUTTON_2_GPIO        13           # GPIO13 (D7) - Подсветка

// Сервер для отправки данных
#define SERVER_URL "http://your-server.com/api/data"
```

### Файл конфигурации устройства
Создайте файл `config.txt` в SPIFFS с параметрами:
```
Hydra-L-001                    # Имя устройства
your_api_key                   # API ключ для сервера
your_wifi_ssid                 # SSID WiFi сети
your_wifi_password             # Пароль WiFi
```

## 🚀 Быстрый старт

### 1. Клонирование проекта
```bash
git clone https://github.com/Unk1nd77/hydra-l-esp8266.git
cd hydra-l-esp8266
```

### 2. Установка окружения разработки

**На macOS:**
```bash
./install_macos.sh
source ~/esp/setup_env.sh
```

**На Linux:**
```bash
./install_linux.sh
source ~/esp/setup_env.sh
```

### 3. Сборка и прошивка
```bash
# Тестирование окружения (опционально)
./test_build.sh

# Настройка проекта (опционально)
./menuconfig.sh

# Сборка проекта (автоматически исправляет проблемы совместимости)
./build.sh

# Прошивка устройства
# macOS:
python3 $IDF_PATH/tools/idf.py -p /dev/cu.usbserial-* flash monitor

# Linux:
python3 $IDF_PATH/tools/idf.py -p /dev/ttyUSB0 flash monitor
```

### 🔧 Настройка проекта (menuconfig)

Для тонкой настройки параметров ESP8266 используйте menuconfig:
```bash
# Запуск интерактивной настройки
./menuconfig.sh

# Или напрямую (после установки ncurses)
python3 $IDF_PATH/tools/idf.py menuconfig
```

**Основные разделы настройки:**
- **Serial flasher config** - настройки прошивки и портов
- **Partition Table** - размеры разделов памяти
- **Component config → ESP8266-specific** - частота CPU, WiFi настройки
- **Component config → FreeRTOS** - настройки операционной системы
- **Component config → Wi-Fi** - параметры WiFi
- **Component config → HTTP Server** - настройки веб-сервера

### 🔧 Устранение проблем совместимости

Если возникают ошибки с CMake, проект автоматически применяет исправления. 
Для ручного исправления выполните:
```bash
./fix_sdk_compatibility.sh
```

### 3. Первоначальная настройка
1. После прошивки устройство создаст точку доступа **Hydra-L**
2. Подключитесь к ней с паролем **12345678**
3. Откройте браузер и перейдите на **192.168.4.1**
4. Настройте параметры WiFi и сервера

## 🔧 Архитектура прошивки

### Многозадачная структура (FreeRTOS)
```c
// Основные задачи системы
sensor_task()     # Чтение данных BME280 каждые 5 сек
lcd_task()        # Обновление LCD дисплея каждые 2 сек  
server_task()     # Отправка данных на сервер каждые 60 сек
button_task()     # Обработка нажатий кнопок с debounce
```

### Система усреднения данных
- **Скользящее среднее** по 5 последним измерениям
- **Фильтрация шумов** для стабильных показаний
- **Автоматическая калибровка** BME280

### Сетевая архитектура
- **Dual Mode WiFi**: одновременно STA (клиент) и AP (точка доступа)
- **Автопереподключение** при потере сети
- **HTTP сервер** с поддержкой JSON API
- **Отказоустойчивость** при сбоях сети

## 🛠️ Разработка и модификация

### Добавление новых сенсоров
1. Создайте новый компонент в `components/your_sensor/`
2. Реализуйте драйвер по образцу BME280
3. Добавьте инициализацию в `main.c`
4. Интегрируйте в `sensor_task()`

### Изменение веб-интерфейса
1. Модифицируйте обработчики в `main.c`
2. Добавьте новые endpoints в структуру `httpd_uri_t`
3. Обновите JSON формат в `data_handler()`

### Настройка GPIO
- Все пины настраиваются в начале `main.c`
- I2C пины: изменить `I2C_MASTER_SCL_IO` и `I2C_MASTER_SDA_IO`
- Кнопки: изменить `BUTTON_1_GPIO` и `BUTTON_2_GPIO`

## 🐛 Устранение неисправностей

### Проблемы сборки
```bash
# Проверка окружения
./test_build.sh

# Очистка и пересборка
rm -rf build/ sdkconfig
./build.sh
```

### Проблемы с устройством
| Проблема | Причина | Решение |
|----------|---------|---------|
| LCD не отображает данные | Неправильное подключение I2C | Проверить SDA/SCL пины |
| BME280 не найден | Адрес I2C или подключение | Проверить адрес 0x76/0x77 |
| WiFi не подключается | Неверные учетные данные | Проверить SSID/пароль |
| Кнопки не работают | GPIO конфигурация | Проверить номера пинов |
| Данные не отправляются | Сетевые проблемы | Проверить URL сервера |

### Отладка через Serial Monitor
```bash
# Подключение к устройству для отладки
python3 $IDF_PATH/tools/idf.py -p /dev/ttyUSB0 monitor

# Полезные команды в мониторе
Ctrl+] - выход из монитора
Ctrl+T Ctrl+R - перезагрузка ESP8266
Ctrl+T Ctrl+H - справка по командам
```

## 📊 Технические характеристики

### Производительность
- **CPU**: ESP8266 @ 80MHz
- **RAM**: ~50KB доступно для приложения
- **Flash**: 4MB (программа ~800KB)
- **WiFi**: 802.11 b/g/n 2.4GHz

### Энергопотребление
- **Активный режим**: ~80mA @ 3.3V
- **Режим сна**: ~20µA (при реализации)
- **Питание**: 3.3V или 5V через USB

### Точность измерений
- **Температура**: ±1°C (BME280)
- **Влажность**: ±3% RH (BME280)  
- **Давление**: ±1 hPa (BME280)
- **Частота измерений**: каждые 5 секунд

## 🤝 Участие в разработке

### Как внести вклад
1. **Fork** репозитория
2. Создайте **feature branch**: `git checkout -b feature/amazing-feature`
3. **Commit** изменения: `git commit -m 'Add amazing feature'`
4. **Push** в branch: `git push origin feature/amazing-feature`
5. Создайте **Pull Request**

### Стиль кода
- Используйте **4 пробела** для отступов
- **Комментируйте** сложные участки кода
- Следуйте **ESP-IDF coding style**
- **Тестируйте** изменения перед commit

## 📄 Лицензия

Этот проект распространяется под лицензией **MIT License** - см. файл [LICENSE](LICENSE) для деталей.

## 🆘 Поддержка и контакты

- **Issues**: [GitHub Issues](https://github.com/Unk1nd77/hydra-l-esp8266/issues)
- **Discussions**: [GitHub Discussions](https://github.com/Unk1nd77/hydra-l-esp8266/discussions)
- **Wiki**: [Project Wiki](https://github.com/Unk1nd77/hydra-l-esp8266/wiki)

---

**Hydra-L** - надежная IoT метеостанция для мониторинга окружающей среды 🌡️💧📊